// pages/index.tsx
import { Containers } from "@/components";

export default function Project() {
  return (
    <Containers.Layout name={"Market"}>
      <Containers.Section el={<>Project</>} />
    </Containers.Layout>
  );
}
